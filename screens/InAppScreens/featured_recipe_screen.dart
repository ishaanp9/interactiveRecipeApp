import 'package:flutter/material.dart';

class FeaturedRecipes extends StatelessWidget { // the featured recipe screen, yet to be completed
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Featured Recipes"),
      ),
      body: Center(
        child: Text("Featured Recipes"),
      ),
    );
  }
}
